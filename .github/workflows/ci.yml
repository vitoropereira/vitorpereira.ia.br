name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Sem `version:` de propósito: a action lê o `packageManager` do
      # package.json. Declarar os dois faz ela abortar com "Multiple versions
      # of pnpm specified".
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      # velite gera .velite/ — typecheck e testes importam daí
      - run: pnpm exec velite build
      - run: pnpm run typecheck
      - run: pnpm run lint
      - run: pnpm test
